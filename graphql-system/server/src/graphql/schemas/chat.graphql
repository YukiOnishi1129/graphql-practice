extend type Query {
  chat: Chat!
}

# extend type Mutation {

# }

type Chat {
  id: Int!
  friend: User!
  userId: Int!
  statement: [Statement!]!
  createdAt: DateTime!
}

type Statement {
  id: Int!
  user: User!
  content: String!
  createdAt: DateTime!
}
